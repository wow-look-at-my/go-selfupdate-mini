name: Build and test

on:
  push:

permissions:
  id-token: write
  contents: read

jobs:

  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - uses: wow-look-at-my/go-toolchain@v1
